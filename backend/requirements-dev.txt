pytest>=9.0.0
pytest-asyncio>=0.23.0
anyio>=4.0.0
ruff>=0.15.0
# Required by starlette/fastapi TestClient (used in tests). Previously present only
# transitively via openai, which is no longer a dependency.
httpx>=0.27.0
