# Group AI Digest Plan

## Steps

1. Add a dedicated group digest CLI that prints an external-agent task for multiple projects.
2. Require exactly three novel generated WebP image files before materializing the group project.
3. Require every group chapter to include facts, theory, and hypothesis fields.
4. Reject overly extractive drafts so the group transcript is novel rather than source concatenation.
5. Create group digest projects without copying source folders so original frames cannot leak into output.
6. Add group lineage and image metadata to `archive.json` and `manifest.json`.
7. Add dashboard project selection and `Copy Group AI Digest CLI Task`.
8. Add dashboard and reader badges for `group_ai_digest`.
9. Hide slicer-based image replacement controls for group digests because their images are generated WebP teaching assets, not source frames.
10. Update playground architecture notes for the group digest flow.
11. Verify CLI task printing, draft validation, project materialization, frontend typechecking, and browser behavior.
